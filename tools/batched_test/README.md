# Auto Test Tutorial

1. Check and edit the `mathine_config.py` with your machine attributes.

2. Update your model configs in `model_config` folder.

3. Run command:

```bash
# If you are in PIP(conda) environments
python schedular.py

# If you are in UV environments
UV=1 python schedular.py
```